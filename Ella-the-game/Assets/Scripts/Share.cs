using System.Collections.Generic;
using UnityEngine;
//using Facebook.Unity;
using UnityEngine.UI;
using System.IO;

public class Share : MonoBehaviour {
    /*private void Awake()
    {
        if (!FB.IsInitialized)
        {
            FB.Init();
        }
        else
        {
            FB.ActivateApp();
        }
    }
    //SHARE Method
    public void facebookShare()
    {
        FB.ShareLink(
            contentTitle: "Best game ever",
            contentURL: new System.Uri("https://www.google.com/"),
            contentDescription: "THis is the best description",
            photoURL: new System.Uri("https://www.google.com/images/branding/googlelogo/1x/googlelogo_color_272x92dp.png"),
            callback:OnShare
            );
        
    }
    private void OnShare(IShareResult result)
    {
        if(result.Cancelled || !string.IsNullOrEmpty(result.Error))
        {
            Debug.Log("ShareLink Error: " + result.Error);
        }else if (!string.IsNullOrEmpty(result.PostId))
        {
            Debug.Log("Post Id: " + result.PostId);
        }
        else
        {
            Debug.Log("Share Successed" );
        }
    }
 */
   
}
